{
  "name": "brick_game",
  "private": true,
  "description": "tile-matching puzzle game",
  "version": "0.5.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "prettier": "prettier --write \"./src/**/*.{js,ts}\"",
    "dev": "vite --open",
    "preview": "vite preview --open --outDir=docs --base=./",
    "build": "vite build --outDir=docs --base=./"
  },
  "dependencies": {
    "@keiii/k-stream": "^0.0.43",
    "prettier": "^3.5",
    "vite": "^4.2.1"
  }
}
