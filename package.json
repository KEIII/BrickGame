{
  "name": "brick_game",
  "private": true,
  "description": "tile-matching puzzle game",
  "version": "0.5.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "prettier": "prettier --write \"./src/**/*.{js,ts}\"",
    "dev": "vite --open",
    "preview": "vite preview --open --outDir=docs",
    "build": "vite build --outDir=docs"
  },
  "dependencies": {
    "prettier": "^2.8.6",
    "vite": "^4.2.1"
  }
}
